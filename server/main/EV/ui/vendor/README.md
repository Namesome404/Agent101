# Vendored browser runtime

`model-viewer.min.js` is `@google/model-viewer` 4.3.1, vendored so the desktop
surface does not depend on a CDN that may be blocked by the WebView CSP or an
offline network. Its BSD-3-Clause license is in `MODEL_VIEWER_LICENSE`.

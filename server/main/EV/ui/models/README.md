# EV offline demo model

`ev-demo-robot.glb` is an original project asset generated by
`generate_ev_demo_model.py`. It is used only when the user asks to display an
unspecified 3D model and search does not return a directly previewable GLB or
glTF file.
